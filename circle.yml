machine:
  node:
    version: 7.3.0
dependencies:
  override:
    - sudo ln -s "$(which node)" /usr/bin/node
    - git config --global user.email "ksespinola@gmail.com"
    - git config --global user.name "Kyle Espinola"
    - npm i
    - npm run build:test
