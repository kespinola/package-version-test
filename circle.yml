machine:
  node:
    version: 7.3.0
dependencies:
  override:
    - git config --global user.email "ksespinola@gmail.com"
    - git config --global user.name "Kyle Espinola"
    - npm i
deployment:
  production:
    branch: master
    commands:
      - npm run release
